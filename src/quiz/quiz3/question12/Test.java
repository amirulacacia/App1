package quiz.quiz3.question12;

public class Test {
    public static void main(String[] args) {
        //boolean b1 = 0;
        //boolean b2 = 1;
        //System.out.println(b1 + b2);
    }
}
/*
 * n Java language, boolean type can store only two values: true and false and
 * these values are not compatible with int type.
 * 
 * Also + operator is not defined for boolean types. Hence, all the 3 statements
 * inside main method causes compilation erro
 */