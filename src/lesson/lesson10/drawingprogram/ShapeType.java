package lesson.lesson10.drawingprogram;

// Making enumeration for shape type.
// Enumeration is a special data type that represents a fixed set of constants.
public enum ShapeType {
    LINE, RECTANGLE, CIRCLE, FREEFORM
}
